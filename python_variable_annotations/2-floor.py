#!/usr/bin/env python3
"""has a functions that returns the floor of a float"""


def floor(n: float) -> int:
    """returns the floor of a float using math.floor"""
    import math
    return math.floor(n)
